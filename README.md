# Finance Backend Project

This project is a simple backend for managing financial records.

Features:
- Add income and expense records
- View all records
- Dashboard summary (total income, expense, balance)

How to run:
npm install express
node app.js

APIs:
POST /add-record
GET /records
GET /summary
